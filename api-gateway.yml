server:
  port: 8090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/
spring:  
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
      routes[0][0]:
        - id: client-service
          uri: lb://CLIENT-SERVICE
          predicates:
            - Path=/business/**,/personnel/**
